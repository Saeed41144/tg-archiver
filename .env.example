# Telegram Bot Token (from @BotFather)
BOT_TOKEN=REPLACE_ME

# Archive channel id (e.g. -1001234567890) — must be PRIVATE
CHANNEL_ID=REPLACE_ME

# First owner user_id (added manually once). Find via @userinfobot
OWNER_ID=REPLACE_ME

# Optional: AI keys for future classifier
# GEMINI_API_KEY=
# OPENAI_API_KEY=
