# 🗂️ ربات آرشیو تلگرام (AI Auto-Archiver Bot)

## 📖 نمای کلی (Vision)

یک ربات تلگرام که **عکسها و فایلها** را بهصورت خودکار و **منظم** در یک کانال/گروه
ذخیره میکند. هوش مصنوعی **نوع دستهبندی هر فایل را خودش تشخیص میدهد** (مثلاً
کارت ملی، پاسپورت، قرارداد، رسید، مدرک تحصیلی، عکس شخصی و...).

در آینده میتوانیم به این ربات «ایجنت»های مختلف وصل کنیم: مثلاً به ربات بگیم
«عکس کارت ملیم رو بیار» و ربات با جستجوی هوشمند فایل موردنظر را برمیگرداند.

---

## 🎯 اهداف (Goals)

1. دریافت عکس/فایل از کاربر و دستهبندی خودکار توسط AI
2. ذخیره منظم در یک کانال/گروه (خود کانال همان «دیتابیس» است)
3. امکان جستجو و بازیابی هوشمند (Retrieval) — «کارت ملیم رو بده»
4. معماری ماژولار که «ایجنتهای» مختلف در آینده به آن وصل شوند
5. قابل اجرا روی Termux (موبایل) — Node.js

---

## 🧠 معماری کلی (Architecture)

```
┌────────────┐     ┌───────────────────┐     ┌─────────────────────┐
│  کاربر/Agent│────▶│  Telegram Bot      │────▶│  AI Classifier      │
│ (سؤالات&فایلها)│     │  (Node.js)        │     │  (LLM / Vision)     │
└────────────┘     └───────────────────┘     └─────────────────────┘
        ▲                   │                          │
        │                   ▼                          ▼
        │            ┌───────────────────┐     ┌─────────────────────┐
        └────────────│  Channel/Gruup    │◀────│  Index/Metadata     │
           بازیابی فایل│  (ذخیره فایل‌ها)  │     │  (نوع/برچسب/تاریخ)   │
                     └───────────────────┘     └─────────────────────┘
```

### اجزای اصلی

| مؤلفه | نقش |
|-------|------|
| **Telegram Bot** | دریافت فایل، ارسال به کانال، پاسخ به کاربر |
| **AI Classifier** | تشخیص نوع فایل از روی تصویر/نام/محتوا |
| **Channel/Gruup** | محل ذخیره فایلها (message_id = شناسه فایل) |
| **Index (JSON/DB)** | ثبت متادیتا: نوع، برچسب، تاریخ، message_id |
| **Retriever Agent** | جستجوی «مدرک X را بیاور» و برگرداندن فایل |

---

## 🗄️ چطور «کانال» دیتابیس میشود؟

- هر عکس/فایلی که کاربر میفرستد، ربات آن را به **کانال آرشیو** پست میکند
  (با عنوان استاندارد مثل `#card_id`).
- کانال بهصورت **Private** است تا فقط ربات و ما ببینیم.
- ربات در یک فایل **index.json** ذخیره میکند:
  - `message_id` (شناسه پست در کانال)
  - `category` (نوع مدرک: کارت ملی، پاسپورت، ...)
  - `tags` (برچسبهای اضافی)
  - `timestamp` (تاریخ)
  - `caption` (توضیح)

> ✅ مزیت: کانال تلگرام رایگان، امن، همیشه آنلاین و پشتیبانگرفتهشده است.

---

## 🤖 دستهبندی توسط AI

دو روش برای تشخیص نوع فایل:

### روش A — Vision (پیشنهادی برای عکس)
مدل هوش مصنوعی تصویری (مثل GPT-4V / Gemini Vision) خود عکس را میبیند و
تشخیص میدهد: «این کارت ملی است».
- مناسب: کارت ملی، پاسپورت، مدارک، اسناد، فاکتور، عکس شخصی

### روش B — بر اساس نام/محتوا (برای PDF و فایل)
- خواندن نام فایل + در صورت امکان متن داخل PDF (با pdftotext)
- پسوند فایل (PDF, JPG, PNG...)
- اگر مدل آفلاین باشد، یک **فهرست کلیدواژه** دستی بهعنوان fallback

> ⚙️ استراتژی پیشنهادی: اول روش A؛ اگر در دسترس نبود، روش B.

---

## 🗂️ دستهبندیهای پیشنهادی (Categories)

| دسته | نمونه |
|------|-------|
| `کارت_ملی` | عکس کارت ملی |
| `پاسپورت` | پاسپورت |
| `شناسنامه` | شناسنامه |
| `مدرک_تحصیلی` | مدرک دانشگاهی/دیپلم |
| `قرارداد` | قرارداد کاری/اجاره |
| `فاکتور` | فاکتور/رسید خرید |
| `قبض` | قبوض برق/گاز/تلفن |
| `تحویل` | کارت بانکی/رمز (مهم: امنیت!) |
| `عکس_شخصی` | عکسهای شخصی |
| `سایر` | هر چیز دیگری |

---

## 🔍 بازیابی هوشمند (Retrieval / Agents)

وقتی به ربات بگویی: *«عکس کارت ملیم رو بیار»*:

1. **Text-to-Category**: ربات جملهات را با AI تفسیر میکند → `کارت_ملی`
2. **Search Index**: دنبال متادیتای مربوطه در `index.json` میگردد
3. **Fetch**: فایل را از کانال با `message_id` برمیدارد و برایت میفرستد

### ایجنتها (Agents) در آینده
معماری را طوری میسازیم که هر «ایجنت» یک رابط مشخص داشته باشد:

```
Agent Registry (ثبت ایجنت‌ها)
 ├── 🪪 Identity Agent      → مدارک هویتی (کارت ملی، پاسپورت، شناسنامه)
 ├── 💼 Finance Agent       → قرارداد، فاکتور، قبض، رسید
 ├── 🏠 Property Agent      → سند ملک، اجاره‌نامه
 └── 🧾 Documents Agent     → هر مدرک دیگر
```

هر ایجنت فقط مسئول دستهبندیهای خودش است → ربات هوشمندتر و سریعتر میشود.

---

## 📁 ساختار پروژه (Project Tree)

```
tg-archiver/
├── package.json
├── .env                  (توکن ربات، ID کانال، کلید AI)
├── src/
│   ├── bot.js            (راه‌اندازی ربات + هندلر پیام‌ها)
│   ├── config.js         (خواندن تنظیمات از .env)
│   ├── classifier/
│   │   ├── ai.js         (اتصال به Vision AI)
│   │   └── keywords.js   (روش fallback با کلیدواژه)
│   ├── storage/
│   │   ├── channel.js    (ارسال/دریافت فایل از کانال)
│   │   └── index.js      (خواندن/نوشتن index.json)
│   ├── retrieval/
│   │   └── search.js     (جستجوی «مدرک X» و برگرداندن)
│   └── agents/
│       └── registry.js   (ثبت و مدیریت ایجنت‌ها)
└── data/
    └── index.json        (متادیتای فایل‌ها)
```

---

## 📦 تکنولوژیها

| نیاز | گزینه | دلیل |
|------|-------|------|
| زبان | **Node.js** | سبک، در Termux عالی، کتابخانههای زیاد |
| کتابخانه ربات | **node-telegram-bot-api** | ساده و پرکاربرد |
| ذخیره متادیتا | **JSON file** | بدون نیاز به نصب دیتابیس (شروع ساده) |
| (اختیاری بعداً) | **SQLite** | وقتی داده زیاد شد |
| AI Vision | **Gemini / OpenAI** | تشخیص تصویر دقیق |
| (اختیاری) | **Ollama/Llama** (محلی) | اجرای AI روی خود گوشی |

---

## 🚧 فازبندی (Roadmap)

| فاز | کار | وضعیت |
|-----|-----|-------|
| **۱** | راهاندازی bot، دریافت فایل، ارسال به کانال با caption | 
| **۲** | index.json: ثبت متادیتا (نوع، تاریخ، message_id) | 
| **۳** | AI Classifier (Vision) برای تشخیص خودکار دسته | 
| **۴** | جستجوی ساده با دستور (مثلاً `/get کارت ملی`) | 
| **۵** | فارسیسازی دستورات و UI | 
| **۶** | ایجنتها: تقسیم دستهبندی به ایجنتهای تخصصی | 
| **۷** | جستجوی هوشمند با AI (جمله طبیعی → فایل) | 

---

## ✅ قدم اول (فاز ۱) — چی میسازیم؟

یک ربات پایه که:
1. عکس/فایل را از کاربر میگیرد
2. یک دسته موقت (مثلاً «سایر» یا بر اساس نام فایل) تعیین میکند
3. به کانال آرشیو پست میکند
4. message_id و متادیتا را در index.json ذخیره میکند
5. به کاربر کد/شناسه میدهد

> بعداً فقط ماژول AI Classifier جایگزین «دسته موقت» میشود.

---

## 🔐 کنترل دسترسی چندحسابه (Access Control) — مهم‌ترین بخش!

**نگرانی:** اگر حساب تلگرامم حذف شود، از حسابِ دیگر به فایل‌هایم برسم، ولی هرکسی که آیدی ربات را بداند نتواند وارد شود.

### ✅ راه‌حل: Allowlist (فهرست مجاز) با شناسه عددی

- هر کاربر تلگرام یک **شناسه عددی یکتا (user_id)** دارد که **قابل حدس نیست**
  (مثل `123456789`). این با «یوزرنیم» فرق دارد — یوزرنیم قابل حدس/تقلب است، user_id نه.
- ربات فقط به کاربرانی که user_id آن‌ها در **allowlist.json** باشد پاسخ می‌دهد.
- هر کس دیگر هر دستوری بدهد → «⛔ شما مجاز نیستید» و نادیده گرفته می‌شود.

### 🧠 چرا هکر یا غریبه نمی‌تواند خودش را اضافه کند؟

ربات روی **دستگاه خود ما** (Termux) اجرا می‌شود. کدِ ربات همیشه چک می‌کند:
> «آیا این user_id در allowlist است؟» اگر نه → رد. 

برای اضافه‌کردن یک کاربر، باید **فایل `allowlist.json` را مستقیماً ویرایش کنیم**
(توسط صاحب دستگاه). یعنی غریبه فقط با «دانستن آیدی ربات» هیچ‌کاری نمی‌تواند بکند،
چون مسیر تنظیمش دستِ ماست، نه روی یک سرور مشترک.

### 🧭 سناریوی «حسابم حذف شد» — چطور وصل شوم؟

دفاعی سه‌لایه تا همیشه راهِ ورود داشته باشیم:

1. **مالکیت دستگاه (راه اصلی):** ربات روی گوشی/دستگاه خودمان اجرا می‌شود.
   حتی اگر همه اکانت‌های تلگرام حذف شوند، ما به فایل‌ها و allowlist دسترسی
   فیزیکی داریم. account جدید را به allowlist اضافه می‌کنیم و تمام.

2. **دستور مالک با «کلید مخفی» (Owner Secret):** یک رمز مخفی نصب‌شده در
   `config.json` داریم. با حساب جدید، پیام `🔒/auth <رمز>` می‌فرستیم؛
   اگر رمز درست بود، user_id همان حساب به allowlist اضافه می‌شود.
   (این راه را فقط وقتی توصیه می‌کنیم که مطمئن باشی رمز لو نرفته.)

3. **بکاپ منظم:** `index.json` و `allowlist.json` هر روز به‌صورت خودکار
   (مثلاً با یک اسکریپت) بکاپ گرفته می‌شود تا هیچ‌وقت نگران نباشیم.

### ➕ افزودن کاربر از خودِ ربات (به‌جای ویرایش دستی فایل)

نخواستی مجبور باشی فایل را دستی ویرایش کنی. پس **همه‌چیز از داخل خودِ ربات**
انجام می‌شود (بدون خروج از تلگرام)، فقط با یک‌بار تأیید اولیه:

```
/addfamily 123456789   ← صاحب (owner) اول اضافه می‌شود (دستی در config)
/adduser @username_ایکس  ← از این به بعد، با یوزرنیم اضافه می‌کنی
```

- **اضافه‌کردن:** فقط **owner** می‌تواند `/adduser @username` بدهد.
  ربات یوزرنیم را به `user_id` عددی تبدیل می‌کند (از طریق Telegram API) و
  به allowlist اضافه می‌کند: `✅ کاربر @x اضافه شد`.
- **حذف‌کردن:** فقط owner: `/removeuser @username`
- **مشاهده لیست:** فقط owner: `/listusers`

> 🔐 چرا این روش امن است؟ چون این دستورها **فقط روی user_id صاحب** کار
> می‌کنند. ربات قبل از هر دستور چک می‌کند که فرستنده در `owners` باشد؛
> اگر نبود → `⛔ فقط مالک`.

```json
// allowlist.json
{
  "owners": [123456789],      // فقط این‌ها می‌توانند /adduser بزنند
  "users": [987654321]        // بقیه فقط می‌توانند فایل بفرستند/دریافت کنند
}
```

### 🔒 قوانین امنیتی طلایی

- **هرگز** به یوزرنیم اعتماد نکنیم — همیشه به user_id عددی چک کنیم
  (یوزرنیم قابل تغییر/بازیافت توسط دیگران است!)
- کانال آرشیو **Private** باشد.
- توکن ربات، Owner Secret و کلید AI در `.env` — در گیتهاب **هرگز** کامیت نشود.
- افزودن هر کاربرِ جدید ثبت (log) شود.

```json
// allowlist.json
{
  "owners": [123456789],
  "users": [987654321]
}
```

| سناریو | نتیجه |
|--------|-------|
| غریبه آیدی ربات را دارد | ❌ رد — user_id در allowlist نیست |
| حساب اصلی حذف شد، از حساب ب جدید از دستگاه خودم | ✅ با ویرایش allowlist یا Owner Secret اضافه می‌شود |
| حساب دزدیده/رمز لو رفته | ❌ صاحب دستگاه می‌تواند آن را حذف کند |

---

💡 **آمادهای شروع کنیم؟** اگر تأیید کنی، فاز ۱ را همینجا در Termux پیادهسازی میکنم و یک ربات واقعی هم میزنیم.
